# ผลตรวจและขอบเขตข้อมูล — 2026-09-08

## พฤติกรรมใหม่

คำตอบเว็บและ LINE ใช้การเลือกหัวข้อและแสดง description, syntax, example จาก dataset โดยตรง
ไม่มีการเรียก Gemini เพื่อสร้างเนื้อหาในเส้นทางตอบปัจจุบัน จึงไม่เพิ่มข้อเท็จจริงหรือ SQL นอกไฟล์
ไม่สร้างคิวรีใหม่ตามโจทย์: แสดงได้เฉพาะตัวอย่างที่จัดเก็บไว้
การคัดข้อความป้องกันการแต่งเติม แต่ไม่ได้รับประกันว่าการค้นหาจะตรงทุกคำถาม หรือข้อมูลต้นฉบับถูกทั้งหมด
ฉบับรวมใช้การแยกข้อความใหม่ที่เก็บทุกบรรทัดเนื้อหา ไม่ใช้ตัวแปลงเดิมที่จำกัดตัวอย่าง 4 รายการ
ความยาวคำตอบเทียบความยาวบทความไม่ใช่ตัววัดความแม่นยำ

## แหล่งข้อมูล

แหล่งข้อมูลที่แอปอ่านเพียงไฟล์เดียว: knowledge/sql_handbook_th.md รวม 99 หัวข้อ
73 ส่วนจากต้นฉบับ 72 หน้า (รวมบทนำและสรุป) ซึ่งหน้าปกระบุ W3Schools
26 หัวข้อเสริมจาก PostgreSQL documentation, Microsoft Learn, OWASP และ MongoDB
ไฟล์ MD รุ่นเก่าเก็บไว้สำหรับตรวจย้อนกลับและสร้างเอกสาร แต่ loader ไม่โหลดซ้ำเมื่อพบฉบับรวม
แต่ละหัวข้อมี URL หน้าเอกสารและวันที่ตรวจ ตัวอย่างสั้นจัดทำขึ้นเพื่อสาธิตและติดป้ายไว้
PostgreSQL syntax ไม่ควรถูกนำไปอ้างว่าใช้ได้ทุก DBMS

## การทดสอบ

รัน `./venv/bin/python -m unittest test_knowledge test_handbook_app -v` ผ่าน 6 tests
ตรวจ PDF 100 หน้าจริง และข้อความ 72 หน้าเดิมตรงต้นฉบับทั้งหมด เก็บ checksum ใน output/pdf/manifest.json
ตรวจการจัดหน้า supplement ทั้ง 28 หน้าไม่ทับ footer และตรวจภาพหน้าปก ภาคเสริม และบรรณานุกรม
Playwright ใช้ API จำลองตรวจสถานะค้นหา ส่งข้อความ รีเฟรชแล้วประวัติยังอยู่ ค้นหัวข้อ และมือถือไม่มี horizontal overflow
ทดสอบ Flask endpoints จริงแยกต่างหากโดย mock การโหลด .env และการบันทึกประวัติ ไม่ใช้ API key
ทดสอบการค้นหัวข้อ 6 กรณี การปฏิเสธคำถามไม่เกี่ยวข้อง 4 กรณี โครงสร้างคำตอบและแหล่งอ้างอิง
ผลเหล่านี้เป็น regression tests ไม่ใช่คะแนนความแม่นยำจากชุดทดสอบอิสระ

## สิ่งที่ยังไม่ครอบคลุม

ยังไม่ใช่สารานุกรมครบทุกเรื่อง: ขาดรายละเอียด vendor-specific ของ Oracle/MySQL/SQL Server,
distributed consensus, tuning เชิงลึก, observability และ migration ใน production
Sharding, NoSQL document modeling และ schema migration มีบทนำแล้ว แต่ยังไม่ใช่คู่มือ production ครบถ้วน
ควรขยายโดยเพิ่มเนื้อหาที่ตรวจแหล่งอ้างอิงแล้วเป็นชุด ๆ และมีคำถามทดสอบประกอบ

## ประวัติและ UX

เก็บประวัติเต็มใน localStorage; จำกัดเฉพาะบริบทที่ส่งไปเซิร์ฟเวอร์ 30 ข้อความ
เพิ่มดาวน์โหลด JSON, แสดงแหล่งข้อมูลในคำตอบ, ลิงก์เอกสารทางการ, focus feedback และ reduced motion
ปิดการเปลี่ยนแชทขณะกำลังรับคำตอบ และเอาปุ่มถามใหม่ที่เดิมแก้ประวัติผิดคู่เวลาเลือกคำตอบเก่าออก
localStorage ไม่ใช่การสำรองถาวรและไม่ซิงก์ข้ามเครื่อง

## สร้างเอกสารซ้ำ

ใช้ Python ที่ติดตั้ง pypdf และ Node ที่ติดตั้ง Playwright/Chromium:

```sh
python build_handbook.py /path/to/chatbot-dataset1.pdf
node render_handbook.cjs
python build_handbook.py /path/to/chatbot-dataset1.pdf --merge
```

ไฟล์ต้นฉบับต้องเป็น PDF 72 หน้าที่ผู้ใช้ส่งมา เอาต์พุตคือ output/pdf/sql_database_handbook_100p_th.pdf
หน้า 1 ปกและสารบัญ, หน้า 2–73 ต้นฉบับเดิม, หน้า 74–99 ภาคเสริม, หน้า 100 บรรณานุกรม
