# ผลตรวจและขอบเขตข้อมูล — 2026-09-23

## แหล่งข้อมูล: ตำรา 426 หน้า / 172 หัวข้อ

แอปอ่าน knowledge/db_business_textbook_th.md เพียงไฟล์เดียว คู่กับ output/pdf/db_business_textbook_th.pdf
(ตำรา "การจัดการระบบฐานข้อมูลเพื่องานธุรกิจ" ฐาปนี เพ็งสุข ม.ราชภัฏอุดรธานี 2562, 426 หน้า)
ไฟล์ PDF ต้นฉบับที่ได้รับชื่อ 2064627N332403C1xCxr.pdf ถูกเปลี่ยนชื่อเท่านั้น เนื้อไฟล์เหมือนเดิม (SHA-256 ใน manifest)

เนื้อหาในเล่ม: 12 บท (ความรู้พื้นฐาน, สถาปัตยกรรม, แบบจำลองเชิงสัมพันธ์, ER Diagram, การแปลงเป็นรีเลชัน,
นอมอลไลเซชัน, SQL CREATE/ALTER/DROP, SELECT และ Aggregate, JOIN, แนวโน้มเทคโนโลยี, ความปลอดภัย)
และภาคผนวก ก (กรณีศึกษาร้านเช่าหนังสือ) ภาคผนวก ข (Microsoft Access)

คู่มือ SQL ฉบับ 126 หน้าเดิม (sql_handbook_th.md และ sql_database_handbook_th.pdf) ถูกลบออกแล้ว

## วิธีแปลง

ดึงข้อความด้วย PyMuPDF (pypdf แทรกช่องว่างกลางคำภาษาไทย จึงไม่ใช้) แล้วแบ่งตามสารบัญ 134 รายการ
หัวข้อทุกหัวข้อหาเจอในเนื้อหาจริง ตัดหัวกระดาษ/เลขหน้า ครอบคำสั่ง SQL ด้วยบล็อก ```sql
หัวข้อที่ยาวเกิน ~3,500 ตัวอักษรแบ่งตามหัวข้อย่อย "1. 2. 3." ในเล่ม หัวข้อย่อยที่ต้นฉบับมีแต่ภาพไม่ถูกสร้าง
หัวข้อ 3.7 และ 5.1 มีชื่อกว้างเกินไป จึงต่อท้ายด้วยชื่อบทที่หัวข้อนั้นอยู่
ไม่ได้แปลง: ภาพและแผนภาพ, บรรณานุกรม, ดัชนี, ประวัติผู้เขียน
บรรทัดที่ PDF ตัดเพราะชนขอบกระดาษ (ดูจากตำแหน่ง x ของบรรทัด) ถูกต่อกลับเป็นย่อหน้า
ช่องตารางที่อยู่แถวเดียวกัน (ตำแหน่ง y เท่ากัน) รวมเป็นบรรทัดเดียวคั่นด้วย " | " หน้าเว็บแสดงเป็นตาราง
เทียบกับฉบับก่อนจัดบรรทัดแล้ว ตัวอักษรเหมือนเดิมทุกหัวข้อ (ไม่นับช่องว่างและตัวคั่น)

## ตรวจว่าคำตอบตรงกับ PDF

ถามบอทด้วยชื่อทุกหัวข้อ แล้วนำทุกบรรทัดเนื้อหาในคำตอบไปค้นในข้อความ PDF (ไม่นับช่องว่าง)
พบใน PDF ครบทุกบรรทัด ที่ไม่ได้มาจาก PDF มีเฉพาะส่วนจัดรูปแบบคงที่ (ชื่อหัวข้อ, บรรทัด "แหล่งข้อมูล:")

## พฤติกรรมการตอบ

คำตอบเว็บ LINE และ Telegram แสดงเนื้อหาหัวข้อจากไฟล์โดยตรง ไม่แสดงเลขหน้า
ไม่เรียก AI ใด ๆ จึงไม่เพิ่มข้อเท็จจริงหรือ SQL นอกไฟล์ และไม่สร้างคิวรีใหม่ตามโจทย์
ไม่มีหัวข้อที่ตรง → ตอบ "ไม่มีข้อมูลเรื่องนี้ในตำราครับ"
ก่อนตอบทุกครั้งตรวจ SHA-256 ของ MD และ PDF เทียบ output/pdf/manifest.json ถ้าไม่ตรงให้หยุดตอบ

การค้นหาเป็น lexical retrieval ใช้ชื่อหัวข้อเท่านั้น มีตารางคำพ้อง (เช่น 3NF, DBMS, weak entity,
นอร์มัลไลเซชัน, HAVING, COUNT) ไม่ค้นในเนื้อหา เพราะคำที่แค่โผล่ในเนื้อหาไม่ได้แปลว่าตำราอธิบายเรื่องนั้น
บทนำ บทสรุป แบบฝึกหัด ถูกลดคะแนน เว้นแต่ผู้ใช้ถามถึงเอง ไม่รับประกันว่าตรงทุกคำถาม

## การทดสอบ

`./venv/bin/python -m unittest test_knowledge test_handbook_app test_dataset_only test_telegram -v` ผ่าน 17 tests
ตรวจทุกหัวข้อว่าคำตอบมีเฉพาะ stored fields, ไม่ทำตาม prompt injection ที่ทดสอบ,
หยุดตอบเมื่อไฟล์หายหรือ checksum ไม่ตรง, คำถามนอกตำรา 12 ข้อ (รวม CTE, Transaction, SQL Injection) ได้ "ไม่มีข้อมูล",
คำถามจริง 15 ข้อเลือกหัวข้อถูก และคำตอบยาวบน Telegram ไม่เกิน 4000 ตัวอักษร
ทดลองคำถามเพิ่มอีก ~90 ข้อระหว่างพัฒนา ข้อที่ยังพลาด เช่น "ตั้งรหัสผ่าน Access" ได้ภาคผนวก ข แทนหัวข้อ 12.5

## แก้ไขเนื้อหาในอนาคต

MD และ PDF ต้องแก้ไปด้วยกัน แล้วอัปเดต markdown_sha256, pdf_sha256, topic_count และ topics ใน manifest.json
ถ้าแก้ไฟล์ใดไฟล์เดียว ระบบจะหยุดตอบตามที่ออกแบบไว้

## สิ่งที่ยังไม่ครอบคลุม

ตำราใช้ Microsoft Access เป็นหลัก ตัวอย่าง SQL บางส่วน (AUTOINCREMENT, NUMBER, เครื่องหมาย ")
เป็นรูปแบบของ Access ไม่ควรอ้างว่าใช้ได้ทุก DBMS
localStorage ไม่ใช่การสำรองถาวรและไม่ซิงก์ข้ามเครื่อง
