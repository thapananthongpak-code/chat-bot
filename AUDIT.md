# ผลตรวจและขอบเขตข้อมูล — 2026-09-18

## แหล่งข้อมูล: คู่มือ 126 หน้า / 123 หัวข้อ

แอปอ่าน knowledge/sql_handbook_th.md เพียงไฟล์เดียว คู่กับ output/pdf/sql_database_handbook_th.pdf (126 หน้า)
หน้า 1 ปกและสารบัญ, หน้า 2–73 ต้นฉบับ 72 หน้าของผู้ใช้ (ระบุ W3Schools), หน้า 74–99 ภาคเสริม 26 หัวข้อ,
หน้า 100 บรรณานุกรม, หน้า 101 สารบัญภาคขยาย, หน้า 102–125 เนื้อหาเพิ่ม 24 หัวข้อ, หน้า 126 บรรณานุกรมเพิ่ม
เลข 100 บนปกเป็นเลขฉบับเก่าที่คงไว้ ไม่ใช่จำนวนหน้าปัจจุบัน

ฉบับเก่า (72 หน้า, 100 หน้า) และสคริปต์สร้างเอกสารถูกลบออกแล้ว เหลือเฉพาะฉบับ 126 หน้า

## พฤติกรรมการตอบ

คำตอบเว็บและ LINE เลือกหัวข้อแล้วแสดง description, syntax, example จากไฟล์โดยตรง
ไม่เรียก Gemini หรือ AI ใด ๆ จึงไม่เพิ่มข้อเท็จจริงหรือ SQL นอกไฟล์ และไม่สร้างคิวรีใหม่ตามโจทย์
ก่อนตอบทุกครั้งตรวจ SHA-256 ของ MD และ PDF เทียบ output/pdf/manifest.json ถ้าไม่ตรงให้หยุดตอบ
เมื่อไม่พบหัวข้อให้แสดงข้อความไม่พบข้อมูล การค้นหาเป็น lexical retrieval ไม่รับประกันว่าตรงทุกคำถาม

## การทดสอบ

`./venv/bin/python -m unittest test_knowledge test_handbook_app test_dataset_only -v` ผ่าน 12 tests
ตรวจทุก 123 หัวข้อว่าคำตอบมีเฉพาะ stored fields, ไม่สร้างตัวอย่างเมื่อไม่มี,
ไม่ทำตาม prompt injection ที่ทดสอบ และหยุดตอบเมื่อไฟล์หายหรือ checksum ไม่ตรง
ทดสอบคำถามจริง 26 ข้อครอบคลุมทุกภาค เลือกหัวข้อถูกทั้งหมด; คำขอให้ "เขียน query" ไม่รองรับ

## แก้ไขเนื้อหาในอนาคต

MD และ PDF ต้องแก้ไปด้วยกัน แล้วอัปเดต markdown_sha256, pdf_sha256 และ topic_count ใน manifest.json
ถ้าแก้ไฟล์ใดไฟล์เดียว ระบบจะหยุดตอบตามที่ออกแบบไว้

## สิ่งที่ยังไม่ครอบคลุม

ยังไม่ใช่เนื้อหาครบทุกฟีเจอร์ของทุก DBMS และตัวอย่างข้ามระบบยังไม่ได้รันกับทุก engine จริง
PostgreSQL syntax ไม่ควรถูกนำไปอ้างว่าใช้ได้ทุก DBMS
localStorage ไม่ใช่การสำรองถาวรและไม่ซิงก์ข้ามเครื่อง
